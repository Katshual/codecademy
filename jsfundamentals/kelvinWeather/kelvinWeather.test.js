import { expect, test } from 'vitest'
import { sum } from './kelvinWeather'

test('adds 1 + 2 to equal 3', () => {
    expect(sum(1,2)).toBe(3)
})

test('kelvin - 273 to equal celsius', () => {
    expect(sum(1,2)).toBe(3)
})

